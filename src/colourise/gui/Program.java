package colourise.gui;

public class Program {
    public static void main(String[] args) {
        // Shows connection dialogue
        Dialogue dialogue = new Dialogue();
        dialogue.setLocationRelativeTo(null);
        dialogue.setVisible(true);
    }
}
